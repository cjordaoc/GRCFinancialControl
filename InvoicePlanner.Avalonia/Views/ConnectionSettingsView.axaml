<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InvoicePlanner.Avalonia.ViewModels"
             xmlns:loc="clr-namespace:App.Presentation.Localization;assembly=App.Presentation"
             mc:Ignorable="d"
             x:Class="InvoicePlanner.Avalonia.Views.ConnectionSettingsView"
             x:DataType="vm:ConnectionSettingsViewModel"
             d:DesignWidth="800"
             d:DesignHeight="450">
    <ScrollViewer>
        <StackPanel Margin="{StaticResource SpaceThickness24}" Spacing="{StaticResource Spacing16}">
            <Border Classes="Card" Padding="{StaticResource SpaceThickness24}">
                <StackPanel Spacing="{StaticResource Spacing16}">
                    <StackPanel Spacing="{StaticResource Spacing8}">
                        <TextBlock Text="{loc:Loc Key=Connection.Section.Language.Title}" Classes="TitleMedium"/>
                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing12}">
                            <TextBlock Text="{loc:Loc Key=Connection.Label.Language}" VerticalAlignment="Center"/>
                            <ComboBox ItemsSource="{Binding Languages}"
                                      SelectedItem="{Binding SelectedLanguage}"
                                      Width="200">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayName}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Spacing="{StaticResource Spacing8}">
                        <TextBlock Text="{loc:Loc Key=Connection.Title.Setup}" Classes="TitleLarge"/>
                        <TextBlock Text="{loc:Loc Key=Connection.Description.Setup}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <Grid ColumnDefinitions="Auto,16,*" RowDefinitions="Auto,Auto">
                        <TextBlock Grid.Row="0"
                                   Text="{loc:Loc Key=Connection.Label.Package}"
                                   VerticalAlignment="Center"
                                   Margin="{StaticResource MarginRightCompact}"/>
                        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" Spacing="{StaticResource Spacing8}">
                            <TextBox Width="320"
                                     Text="{Binding SelectedPackagePath}"
                                     Watermark="{loc:Loc Key=Connection.Placeholder.Package}"
                                     IsReadOnly="True"/>
                            <Button Content="{loc:Loc Key=Connection.Button.Browse}"
                                    Command="{Binding BrowseCommand}"
                                    MinWidth="120"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1"
                                   Text="{loc:Loc Key=Connection.Label.Passphrase}"
                                   VerticalAlignment="Center"
                                   Margin="{StaticResource MarginTopCompactRightCompact}"/>
                        <TextBox Grid.Row="1"
                                 Grid.Column="2"
                                 Margin="{StaticResource MarginTopCompact}"
                                 Text="{Binding Passphrase}"
                                 Watermark="{loc:Loc Key=Connection.Placeholder.Passphrase}"
                                 PasswordChar="*"
                                 MinWidth="240"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing12}">
                        <Button Content="{loc:Loc Key=Connection.Button.Import}"
                                Command="{Binding ImportCommand}"
                                MinWidth="160"/>
                        <TextBlock Text="{Binding SelectedFileName}"
                                   VerticalAlignment="Center"
                                   Opacity="0.7"
                                   IsVisible="{Binding HasSelectedFile}"/>
                    </StackPanel>
                    <Border Background="{StaticResource BrushSurfaceVariant}"
                            CornerRadius="6"
                            Padding="{StaticResource SpaceThickness12}"
                            IsVisible="{Binding HasStatusMessage}">
                        <TextBlock Text="{Binding StatusMessage}"
                                   TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
