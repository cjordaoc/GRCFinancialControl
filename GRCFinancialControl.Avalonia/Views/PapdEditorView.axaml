<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:GRCFinancialControl.Avalonia.ViewModels"
             xmlns:loc="clr-namespace:App.Presentation.Localization;assembly=App.Presentation"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="200"
             x:Class="GRCFinancialControl.Avalonia.Views.PapdEditorView"
             x:DataType="vm:PapdEditorViewModel">
    <Grid RowDefinitions="*,Auto" RowSpacing="{StaticResource GridGap}">
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="{StaticResource SectionSpacing}">
                <TextBlock Text="{loc:Loc Key=FINC_Admin_Papds_Title_Editor}" Classes="TitleSmall"/>
                <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" RowSpacing="{StaticResource GridGap}" ColumnSpacing="{StaticResource GridGap}">
                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="{loc:Loc Key=FINC_Admin_Papds_Placeholder_Name}"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Text="{Binding Name}"
                             Watermark="{loc:Loc Key=FINC_Admin_Papds_Placeholder_Name}"
                             IsReadOnly="{Binding IsReadOnlyMode}"
                             HorizontalAlignment="Stretch"
                             MinWidth="{StaticResource TextBoxMinWidth}"/>

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="{loc:Loc Key=FINC_Admin_Papds_Placeholder_Level}"
                               VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1"
                              Grid.Column="1"
                              ItemsSource="{Binding LevelOptions}"
                              SelectedItem="{Binding Level}"
                              PlaceholderText="{loc:Loc Key=FINC_Admin_Papds_Placeholder_Level}"
                              IsEnabled="{Binding AllowEditing}"
                              HorizontalAlignment="Stretch"
                              MinWidth="{StaticResource ComboBoxMinWidth}"/>

                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Text="{loc:Loc Key=FINC_Admin_Papds_Placeholder_WindowsLogin}"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             Text="{Binding WindowsLogin}"
                             Watermark="{loc:Loc Key=FINC_Admin_Papds_Placeholder_WindowsLogin}"
                             IsReadOnly="{Binding IsReadOnlyMode}"
                             HorizontalAlignment="Stretch"
                             MinWidth="{StaticResource TextBoxMinWidth}"/>

                    <TextBlock Grid.Row="3"
                               Grid.Column="0"
                               Text="Engagement Partner GUI"
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3"
                             Grid.Column="1"
                             Text="{Binding EngagementPapdGui}"
                             Watermark="Engagement Partner GUI"
                             IsReadOnly="{Binding IsReadOnlyMode}"
                             HorizontalAlignment="Stretch"
                             MinWidth="{StaticResource TextBoxMinWidth}"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="{StaticResource ControlSpacing}">
            <Button Content="{loc:Loc Key=Global_Button_Save}"
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding AllowEditing}"
                    IsDefault="{Binding AllowEditing}"
                    MinWidth="{StaticResource ButtonMinWidth}" />
            <Button Content="{loc:Loc Key=Global_Button_Cancel}"
                    Command="{Binding CloseCommand}"
                    IsCancel="True"
                    MinWidth="{StaticResource ButtonMinWidth}" />
        </StackPanel>
    </Grid>
</UserControl>